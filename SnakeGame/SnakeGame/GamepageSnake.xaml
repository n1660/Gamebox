<Page x:Class="MySnake.GamepageSnake"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MySnake"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="SnakeGame.GamepageSnake" Height="400" Width="700" KeyDown="Game_KeyDown">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="99*"/>
            <ColumnDefinition Width="35*"/>
        </Grid.ColumnDefinitions>
        <Canvas Name="GameCanvas" Background="Green">
        </Canvas>
        <Canvas Grid.Column="2" Background="#FFA9EEE4" VerticalAlignment="Top">
            <StackPanel Orientation="Horizontal" Height="100" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Name="txtsc" FontSize="22" FontWeight="Bold" TextAlignment="Center">Score:</TextBlock>
                <TextBlock Name="txtbScore" FontSize="22" FontWeight="Bold" Margin="25, 0 ,0 ,0"  TextAlignment="Center">0</TextBlock>
            </StackPanel>
        </Canvas>
    </Grid>
</Page>
